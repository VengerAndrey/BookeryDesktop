<UserControl x:Class="WPF.Controls.NavigationBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF.Controls"
             xmlns:nav="clr-namespace:WPF.State.Navigators"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="200">
    <StackPanel Orientation="Vertical" Background="{StaticResource BrushPrimary2}">
        <Button Content="{StaticResource ImageFolder}" Style="{StaticResource StyleNavigationButton}"
                Command="{Binding UpdateCurrentViewModelCommand}" CommandParameter="{x:Static nav:ViewType.Containers}"/>
        <Button Content="{StaticResource ImageUser}" Style="{StaticResource StyleNavigationButton}"
                Command="{Binding UpdateCurrentViewModelCommand}" CommandParameter="{x:Static nav:ViewType.Mock}"/>
    </StackPanel>
</UserControl>
